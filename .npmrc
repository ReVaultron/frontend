# Force legacy peer dependency resolution
legacy-peer-deps=true

# Disable strict SSL (only if you have SSL issues)
# strict-ssl=false

# Use public registry
registry=https://registry.npmjs.org/

# Increase fetch timeout
fetch-timeout=60000